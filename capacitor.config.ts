import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonicVideosAppMorenoGerard',
  webDir: 'dist'
};

export default config;
